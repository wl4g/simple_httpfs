<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>Title</title>
    </head>
    <style>
        .container {
            position: relative;
            margin: auto;
            width: 40%;
            height: 300px;
            background-color: #eaf9fa;
            border-radius: 36px;
            box-shadow: 1px 2px 3px #f8d4d4;
        }
        .container .upload-plane {
            position: relative;
            margin-top: 20%;
            width: 20%;
            height: 20%;
            border-radius: 40px;
            margin-left: 38%;
        }
        .container .upload-plane .upload-choose {
            width: 160px;
            height: 50px;
            position: absolute;
            margin-top: 58px;
            background-color: blue;
            text-align: center;
            line-height: 50px;
            border-radius: 30px;
            color: white;
            box-shadow: 2px 2px 3px #ccacac;
            font-weight: 600;
            cursor: pointer;
        }
        .container .upload-plane .upload-file {
            position: relative;
            margin-left: 1px;
            margin-top: 56px;
            width: 160px;
            height: 50px;
            z-index: 300;
            opacity: 0;
            border-radius: 47px;
            cursor: pointer;
        }
        .container .upload-plane .upload-submit {
            cursor: pointer;
            position: relative;
            margin-top: 34%;
            background-color: #518835;
            border-radius: 30px;
            width: 160px;
            height: 50px;
            color: white;
            border: 0;
            box-shadow: 2px 2px 3px #ccacac;
            transition-duration: 0.3s;
            font-weight: 600;
        }
        .container .upload-plane .upload-submit:hover {
            color: red;
        }
    </style>
    <body>
        <div class="container">
            <div class="upload-plane">
                <form ENCTYPE="multipart/form-data" method="post">
                    <a class="upload-choose">Choose file</a>
                    <input class="upload-file" type="file" id="upload_file" />
                    <input type="button" class="upload-submit" value="Uploading"
                        onclick="xhrSubmit()"
                        />
                </form>
            </div>
        </div>
    </body>
</html>